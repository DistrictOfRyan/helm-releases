{
  "version": "1.0.19",
  "url": "https://github.com/DistrictOfRyan/helm-releases/releases/latest",
  "notes": "Fixes HELM going silent after a Windows update. Windows occasionally updates the UI-automation component HELM uses to click Allow, which invalidated HELM's cached copy so approvals silently failed (HELM listed your sessions but never clicked Allow). HELM now detects that exact failure, rebuilds its cached copy against the updated component automatically, and logs the recovery. No reinstall or restart needed. Recommended for everyone."
}