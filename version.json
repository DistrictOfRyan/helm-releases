{
  "version": "1.0.18",
  "url": "https://github.com/DistrictOfRyan/helm-releases/releases/latest",
  "notes": "Fixes a case where quitting HELM left it impossible to reopen. Quitting set a STOP flag that wasn't cleared, so the next launch (including at login) saw the flag and exited immediately. HELM now removes that flag once it has stopped, so reopening works normally from any launcher or at startup. Recommended for anyone on 1.0.17."
}
